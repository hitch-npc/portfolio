# Архитектура: как устроено сейчас

Статус: снимок после R1, 18 сентября 2026. Целевая архитектура и что ещё не сделано — в [05-runway.md](05-runway.md).

## Стек

- Vite 8, чистый JS (ES modules) и CSS. Фреймворка нет.
- Сборка в один файл `dist/index.html` (`vite-plugin-singlefile`): стили и скрипты
  встроены, страницу можно открыть без сервера.
- CSP кладётся мета-тегом при сборке (`vite.config.js`): скрипты пускаются по sha256,
  сетевых запросов у страницы нет.
- Деплой: GitHub Actions (`.github/workflows/pages.yml`) → GitHub Pages,
  перед сборкой гоняется `npm run check`.

## Пайплайн значений

```
tokens.json ──build-tokens──▶ src/styles/tokens.css (:root, руками не правится)
vendor/tts ───build-tts─────▶ ядро кейса TTS под .tts (цвета клиента)
Fontshare ────fetch-fontshare▶ public/fonts/fontshare/ (не в репозитории, лицензия)
                               │
tooling/guardrail.mjs ◀────────┘ страж: сырой #hex вне :root, радиус не из токенов,
                                 анимация без reduced-motion, outline без :focus-visible
```

## Страница

Одна страница, `index.html` (~50 КБ). Порядок сверху вниз:

1. Прелоадер: лента из линз, процент, сменяющиеся строки.
2. Topbar: «The Black Cat Studio™» и «Valencia · ES», переживает прокрутку.
3. Hero: знак TBCS из треугольников на холсте, под ним строка «N triangles. One system.»,
   имя, роль, «Scroll down».
4. Skills: четыре пункта, зрачок (`iris`) повторяет активный.
5. Work: колода из двух карт. TTS с живым макетом, вторая карта «Coming soon».
6. Кейс TTS: `<dialog>`, раскрывается из карты, свёрстан на ядре TTS.

**Контакта на странице нет** (EP-2 ждёт решения Q10). About и CV отложены.

## Модули

| Модуль | Строк | Что делает | Зависит от |
|---|---|---|---|
| `src/main.js` | ~170 | Загрузка: прелоадер → поле → строка с идеей → skills → кейс → work → дешифровка | всё ниже |
| `field/glyph-field.js` | ~630 | Правило поля: покрытие, поток, вихрь, лупа, дыхание и линза инверсии, вдох-выдох, корзины. Отдаёт `markbox` с габаритами и числом фигур | `shapes.js` |
| `field/shapes.js` | ~70 | Фигуры поля: `triangle` (знак), `arrow` (прежняя) | — |
| `motion/frame.js` | 62 | Общий кадр прокрутки: сначала все чтения, потом все записи | — |
| `motion/reduced.js` | 7 | Одна проверка `prefers-reduced-motion` | — |
| `motion/visibility.js` | 13 | Пауза холстов за экраном | — |
| `motion/decrypt.js` | ~125 | Дешифровка; `bindDecrypt` для `[data-decrypt]`; копия для скринридера | `reduced.js` |
| `brand/preloader.js` | 177 | Лента из линз, прогресс | — |
| `sections/skills.js` | ~200 | Skills, прокрутка ведёт активный пункт | `iris.js`, `motion/*` |
| `sections/iris.js` | ~600 | Зрачок на холсте, корона на токенах `--iris-*` | `motion/*` |
| `sections/work.js` | ~310 | Колода карт, сборка заголовка по буквам, окно кейса | `motion/*` |
| `sections/case.js` | 54 | Появление блоков кейса, счётчики | `reduced.js` |
| `sections/gallery.js` | 503 | Круговая галерея, **только в песочнице** | — |
| `sandbox/gallery-lab.js` + `gallery.html` | — | Песочница галереи (`npm run lab`), в сборку не входит | `gallery.js` |
| `sandbox/field-lab.js` + `field.html` | — | Песочница знака (`npm run lab:field`): ручки, пресеты, цена кадра. В сборку не входит | `field/*` |

Стили: `tokens.css` (генерируется), `base`, `hero`, `light` (луч на акцентах), `skills`,
`work`, `case`, `tts-case`, `preloader`, шрифты; песочницы — `lab.css`, `gallery.css`, `field-lab.css`.

## Ограничения, которые держим

- **Доступность.** Для каждого движения есть путь `prefers-reduced-motion`,
  `:focus-visible` описан везде, где снят `outline`. Холсты `aria-hidden`,
  смысл дублируется текстом (`sr-only` у знака).
- **Производительность.** Поле: около 12 800 треугольников на 885 px, 64 корзины
  яркости режутся на пачки по 32 фигуры, кадр поля около 3 мс. Холсты за экраном
  не рисуются (`pauseOffscreen`). Чтения и записи геометрии разведены через `motion/frame.js`.
- **Мобильный Safari.** Секции с трансформами режут горизонталь
  (`overflow-x: clip`), иначе Safari сжимает страницу целиком.
- **Безопасность.** Отладочные ручки (`window.__*`) только в dev и
  вырезаются из сборки. Шрифты свои, Google не участвует.
- **Цвета клиента.** `.tts` живёт на токенах TTS, портфолио на своих.

## Расхождения

Найденные при разборе 18.09 закрыты в R1: README про радиус, три копии проверки
reduced-motion, незакоммиченное рабочее дерево. Открытый долг —
в [реестре](05-runway.md#реестр-техдолга).
